import React from "react";

const Answer = ({rightAnswer}) => {
    return (
        <div >
            <div>
                <h1>{rightAnswer}</h1>
            </div>
        </div>
    );
}

export default Answer
